ARG TAG=latest
FROM docker.io/ecpe4s/e4s-base-rocm:${TAG}

USER root
ENV DEBIAN_FRONTEND=noninteractive
RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y \
       libfftw3-dev libgtest-dev && \
    apt-get purge --autoremove -y && \
    rm -rf /var/lib/apt/lists/*
